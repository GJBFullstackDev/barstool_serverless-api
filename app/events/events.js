/**
 * @desc Media module events
 * @const {Object} MEDIA
 */
const MEDIA = {
  PROCESSED: 'media-processed',
  FAILED: 'media-failed'
}

/**
 * @desc Review module events
 * @const {Object} REVIEW
 */
const REVIEW = {
  CREATED: 'review-created',
  VISIBLE: 'review-visible',
  DELETED: 'review-deleted'
}

module.exports = {
  MEDIA,
  REVIEW
}
